#include <iostream>
#include <vector>
#include "my_utils.h"
namespace phx {
//在cpp 文件中函数也需要定义在名空间中，正如在h文件中也声明在名空间内一样

// 模版函数不能定义在cpp中
// template<typename T>
// void ShowVec(const std::vector<T>& vec)
// {
//     for (const auto e : vec) {
//         std::cout << e << std::endl;
//     }
// }

}